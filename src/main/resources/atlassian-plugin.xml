<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="mattermost-confluence-plugin"/>
    
    <!-- add our web resources -->
    <web-resource key="mattermost-confluence-plugin-resources" name="mattermost-confluence-plugin Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="mattermost-confluence-plugin.css" location="/css/mattermost-confluence-plugin.css"/>
        <resource type="download" name="mattermost-confluence-plugin.js" location="/js/mattermost-confluence-plugin.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>mattermost-confluence-plugin</context>
    </web-resource>
    
</atlassian-plugin>
